let num1 = 4;

// Condicionales if else - si - entonces
// && y
// || ó

// Debemos dividir el número entre dos y luego validar si el resto es cero

if (num1 % 2 === 0) {
  console.log("El número " + num1 + " es par");
} else {
  console.log("El número " + num1 + " es impar");
}


// if else - si - entonces

// es igual y del mismo tipo ===
// es igual (no toma en cuenta el tipo) ==
// mayor que >
// menor que <
// menor o igual <=
// mayor o igual >=
// no es igual (incluyendo el tipo) !==
// no es igual (no toma en cuenta el tipo) !=
// ! no - negar

// && y - cond1 && cond2
// || ó - cond1 || cond2

// condicion ? si se cumple : accion por defecto